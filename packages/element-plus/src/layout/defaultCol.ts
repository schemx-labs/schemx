/**
 * Element Plus 默认 Col 定义。
 *
 * 由 Element Plus 适配包 Runtime 作为默认布局列组件使用；Vue 层提供通用 flex 行容器。
 *
 * @module layout/defaultCol
 */

import { ElCol } from "element-plus"

/** Element Plus 适配包默认 Col。 */
export const ElementPlusCol = ElCol
