# Element Plus 示例

该示例展示 `@schemx/element-plus` 的输入、选项、日期时间、树选择、评分、滑块和上传 Renderer，包括自定义选项字段名。

```bash
pnpm install
pnpm --filter element-plus-demo dev
```

需要直接引用 workspace 源码时，可设置 `VITE_USE_SOURCE=true`。
